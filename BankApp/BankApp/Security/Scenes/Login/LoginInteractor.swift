//
//  LoginInteractor.swift
//  BankApp
//
//  Created by José Inácio Athayde Ferrarini on 17/03/20.
//  Copyright (c) 2020 br.com.solutis.inacioferrarini. All rights reserved.
//
//  This file was generated by the Clean Swift Xcode Templates so
//  you can apply clean architecture to your iOS and Mac projects,
//  see http://clean-swift.com
//

import UIKit

protocol LoginBusinessLogic {
    func doLogin(request: Login.Authentication.Request)
    func isCPFValid(cpf: String) -> Bool
    func isEmailValid(email: String) -> Bool
    func isPasswordValid(password: String) -> Bool
}

protocol LoginDataStore {
    var userData: UserDataModel? { get set }
    var loginError: String? { get set }
}

class LoginInteractor: LoginBusinessLogic, LoginDataStore {
    
    var presenter: LoginPresentationLogic?
    var worker: LoginWorker? = LoginWorker()
    
    var userData: UserDataModel?
    var loginError: String?

    // MARK: Login
  
    func doLogin(request: Login.Authentication.Request) {
        worker?.doLogin(with: request, completion: { (result: Result<Login.Authentication.Response?, Error>) in
            switch result {
            case .success(let response):
                self.userData = response?.userAccount
                self.loginError = nil
                self.presenter?.loginSuccess(response: response)
            case .failure(let error):
                self.userData = nil
                self.loginError = error.localizedDescription
                self.presenter?.loginError(error: self.loginError)
            }

        })
    }
    
    private let regexPassword = "^(?=.*[a-zA-Z0-9])(?=.*[A-Z])(?=.*[^A-Za-z0-9])"
    
    func isCPFValid(cpf: String) -> Bool {
        let cpf = self.onlyNumbers(cpf)
        guard cpf.count == 11 else { return false }

        let i1 = cpf.index(cpf.startIndex, offsetBy: 9)
        let i2 = cpf.index(cpf.startIndex, offsetBy: 10)
        let i3 = cpf.index(cpf.startIndex, offsetBy: 11)
        let d1 = Int(cpf[i1..<i2])
        let d2 = Int(cpf[i2..<i3])

        var temp1 = 0, temp2 = 0

        for i in 0...8 {
            let start = cpf.index(cpf.startIndex, offsetBy: i)
            let end = cpf.index(cpf.startIndex, offsetBy: i+1)
            let char = Int(cpf[start..<end])

            temp1 += char! * (10 - i)
            temp2 += char! * (11 - i)
        }

        temp1 %= 11
        temp1 = temp1 < 2 ? 0 : 11-temp1

        temp2 += temp1 * 2
        temp2 %= 11
        temp2 = temp2 < 2 ? 0 : 11-temp2

        return temp1 == d1 && temp2 == d2
    }
    
    func isEmailValid(email: String) -> Bool {
        let emailRegEx = "[A-Z0-9a-z._%+-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,64}"
        let emailPred = NSPredicate(format:"SELF MATCHES %@", emailRegEx)
        return emailPred.evaluate(with: email)
    }

    private func onlyNumbers(_ cpf: String) -> String {
        guard !cpf.isEmpty else { return "" }
        return cpf.replacingOccurrences(of: "\\D",
                                        with: "",
                                        options: .regularExpression,
                                        range: cpf.startIndex..<cpf.endIndex)
    }
    
    func isPasswordValid(password: String) -> Bool {
        let regex = try! NSRegularExpression(pattern: regexPassword)
        return regex.firstMatch(in: password, options: [], range: NSRange(location: 0, length: password.count)) != nil
    }
    
}
