//
//  LoginWorker.swift
//  TesteIOS
//
//  Created by VM on 26/07/20.
//  Copyright (c) 2020 VM. All rights reserved.
//
//  This file was generated by the Clean Swift Xcode Templates so
//  you can apply clean architecture to your iOS and Mac projects,
//  see http://clean-swift.com
//

import UIKit

typealias LoginResponseSuccess = (_ response: LoginModel.Login.Response) -> ()
typealias LoginResponseFailure = (_ response: String) -> ()


class LoginWorker {
    func createRequest(requestData: LoginModel.Login.Request, urlRequest: URL) -> URLRequest {
        var request = URLRequest(url: urlRequest)
        request.httpMethod = "POST"
        request.httpBody = requestData.getBody().data(using: .utf8)
        
        return request
    }
    
    func validateUserName(username: String) -> Bool {
        return username.isValidCpf() || username.isValidRegex(regex: Constants.emailRegex/*Regex.emailRegex*/)
    }
    
    func validatePassword(password: String) -> Bool {
        return password.isValidRegex(regex: Constants.capitalLetterRegex/*Constants.Regex.capitalLetterRegex*/) && password.isValidRegex(regex: Constants.numberRegex) && password.isValidRegex(regex: Constants.specialCharacterRegex)
    }
    
    func login(requestData: LoginModel.Login.Request, completionSuccess: @escaping LoginResponseSuccess, completionFailure: @escaping LoginResponseFailure) {
        guard let urlRequest = URL(string: Constants.login) else { return }
        let request = createRequest(requestData: requestData, urlRequest: urlRequest)
        
        URLSession.shared.dataTask(with: request) { (data, response, error) in
            guard error == nil else {
                completionFailure(error?.localizedDescription ?? "")
                return
            }
            
            guard let user = User.parse(responseData: data) else {
                completionFailure(error?.localizedDescription ?? "")
                return
            }
            
            completionSuccess(LoginModel.Login.Response(user: user))
        }.resume()
    }
}
