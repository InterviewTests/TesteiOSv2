import UIKit

enum Login {
    // MARK: Use cases
    
    enum LoginModels {
        struct Request {
            let auth: Authentication
        }
        struct Response {
        }
        struct ViewModel {
        }
    }
}
