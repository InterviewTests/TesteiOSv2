use_frameworks!

platform :ios, '12.0'

def podsiOS
  pod 'ServicesLibrary', :path => '../'
  pod 'ModelsLibrary' , :path => "~/Desktop/TesteiOSv2/Projects/Pods/ModelsLibrary"
  pod 'RxSwift', '6.1.0'
  pod 'RxCocoa', '6.1.0'
  # pod 'RxRelay, '6.1.0'
  pod 'RxAlamofire', '6.1.1'
end

def podsiOSTest
  pod 'ServicesLibrary', :path => '../'
end

target 'ServicesLibrary_Example' do
  podsiOS
end

target 'ServicesLibrary_Tests' do
  inherit! :search_paths
  podsiOSTest
 end


post_install do |installer|
   installer.pods_project.targets.each do |target|
      target.build_configurations.each do |config|
         config.build_settings['SWIFT_VERSION'] = '5.0'
      end
   end
end
